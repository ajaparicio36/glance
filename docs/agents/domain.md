# Domain docs

This is a single-context repo. Read root `CONTEXT.md` when it exists.
Read relevant existing ADRs if any are added for the production app.
Missing domain docs are normal; create them only when needed.
Do not create ADRs for the Glance demo.
