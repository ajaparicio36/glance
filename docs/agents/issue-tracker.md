# Issue tracker: Local Markdown

Issues and specs live in `.scratch/<feature-slug>/`.
Use `spec.md` for a feature spec and numbered `issues/NN-slug.md` files for implementation tickets.
Record triage state with a `Status:` line and append discussion under `## Comments`.
