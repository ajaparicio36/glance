@AGENTS.md

## Agent skills

### Issue tracker

Issues and specs live in `.scratch/<feature>/`. See `docs/agents/issue-tracker.md`.

### Triage labels

Use the five default triage role names. See `docs/agents/triage-labels.md`.

### Domain docs

Use a single root context when one is eventually needed. See `docs/agents/domain.md`.
